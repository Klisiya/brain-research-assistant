# Frontend

React and TypeScript interface built with Vite.

See the [project README](../README.md) for setup and backend requirements.

Run from this directory:

```powershell
npm.cmd ci
npm.cmd run dev
```

Checks:

```powershell
npm.cmd run lint
npm.cmd run build
```
